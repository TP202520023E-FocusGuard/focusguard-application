// Servicio para manejar las metas y objetivos
// Por ahora vacío, se integrará con el backend posteriormente

export const goalsService = {
    // Métodos para interactuar con el backend
    // guardarMeta(meta) { ... },
    // obtenerMeta() { ... },
    // guardarObjetivos(objetivos) { ... },
    // obtenerObjetivos() { ... }
};

export default goalsService;